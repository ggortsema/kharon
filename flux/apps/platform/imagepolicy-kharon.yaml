apiVersion: image.toolkit.fluxcd.io/v1beta2
kind: ImagePolicy
metadata:
  name: kharon
  namespace: flux-system
spec:
  imageRepositoryRef:
    name: kharon
  filterTags:
    pattern: '^v(?P<version>.*)$'
    extract: '$version'
  policy:
    semver:
      range: ">=0.0.0"
